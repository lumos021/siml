{
  "name": "siml",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=siml-demo",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  }
}
